/**
 * GitHub Device Flow Configuration Template
 *
 * 1. Create a GitHub OAuth App: https://github.com/settings/applications/new
 *    - Authorization callback URL: http://127.0.0.1 (required field, but not used for Device Flow)
 * 2. Copy this file to config.js
 * 3. Replace CLIENT_ID with your OAuth App's Client ID
 *
 * Note: Device Flow OAuth doesn't require CLIENT_SECRET - it's secure for browser extensions!
 */

export const CLIENT_ID = "YOUR_CLIENT_ID_HERE";
