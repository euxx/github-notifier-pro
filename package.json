{
  "name": "github-notifier-pro",
  "version": "1.0.0",
  "type": "module",
  "description": "A Chrome extension that brings GitHub notifications right to your browser",
  "scripts": {
    "ci": "npm run test && npm run lint:fix && npm run format",
    "dev": "npm run ci && npm run dev:firefox",
    "all": "npm run ci && npm run build",
    "build": "rm -rf dist && mkdir -p dist && zip -r dist/github-notifier-pro-chrome.zip manifest.json src/ images/ -x '*.DS_Store' '*.git*' '*node_modules*' '*.md' && cp manifest-firefox.json manifest.json && zip -r dist/github-notifier-pro-firefox.zip manifest.json src/ images/ -x '*.DS_Store' '*.git*' '*node_modules*' '*.md' && git checkout manifest.json && echo '✨ Build complete! Chrome & Firefox packages in dist/'",
    "dev:firefox": "rm -rf dist/firefox-dev && mkdir -p dist/firefox-dev && cp manifest-firefox.json dist/firefox-dev/manifest.json && cp -r src dist/firefox-dev/ && cp -r images dist/firefox-dev/ && echo '🦊 Firefox dev environment ready at dist/firefox-dev/'",
    "test": "vitest run",
    "lint": "eslint src/ tests/",
    "lint:fix": "eslint src/ tests/ --fix",
    "format": "prettier --write \"src/**/*.{js,html,css}\" \"tests/**/*.js\"",
    "format:check": "prettier --check \"src/**/*.{js,html,css}\" \"tests/**/*.js\"",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.js": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{html,css}": [
      "prettier --write"
    ]
  },
  "keywords": [
    "github",
    "notifications",
    "chrome-extension",
    "webextension"
  ],
  "author": "euxx",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/euxx/github-notifier-pro"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "eslint": "^9.39.2",
    "eslint-config-prettier": "^10.1.8",
    "globals": "^17.2.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.8.1",
    "vitest": "^4.0.18"
  }
}
